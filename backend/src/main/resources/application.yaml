spring:
  datasource:
    url: **************************************************
    username: ******************
    password: ***********************
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

server:
  port: 8080

cors:
  allowed-origins: http://localhost:4200

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.desciptor.sql.BasicBinder: TRACE
    com.elecciones: DEBUG

jwt:
  secret: mi_clave_super_segura_12345678901234567890
  expiration: 36000000